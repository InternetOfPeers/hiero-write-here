# Transaction Payer Configuration
# This account pays for all Hedera transactions
# Get testnet account from: https://portal.hedera.com/register
# For mainnet, use your production account credentials

PAYER_ACCOUNT_ID=0.0.xxxxx
PAYER_PRIVATE_KEY=302e020100xxxxxxxxxxxxxxx # DER format private key of the payer account

# Message Box Owner Configuration
# The account that owns the message box and signs messages to prove ownership
# If the payer wants to own the message box, set the same values as PAYER_* above

MESSAGE_BOX_OWNER_ACCOUNT_ID=0.0.yyyyy
MESSAGE_BOX_OWNER_PRIVATE_KEY=302e020100yyyyyyyyyyyyy # DER format private key of the message box owner account

# Encryption Configuration
# Options: RSA, ECIES
# RSA: Uses RSA-2048 keys (generated and stored in data/ folder)
# ECIES: Uses operator's SECP256K1 key for encryption (derived from HEDERA_PRIVATE_KEY)
# Note: ECIES requires SECP256K1 - ED25519 keys are not supported
ENCRYPTION_TYPE=RSA

# Data Directory for storing keys (used only for RSA encryption)
RSA_DATA_DIR=./data

# Network Configuration
# Options: testnet, mainnet
HEDERA_NETWORK=testnet

# Mirror Node URL. When not set, defaults to:
# Testnet: https://testnet.mirrornode.hedera.com/api/v1
# Mainnet: https://mainnet.mirrornode.hedera.com/api/v1
MIRROR_NODE_URL=https://testnet.mirrornode.hedera.com/api/v1
